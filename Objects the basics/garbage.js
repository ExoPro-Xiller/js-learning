let user1 = { // Global Variable named "user"
    name: "Ahmad", // object {name:"Ahmad",}
};
// Now if we equal the variable to null, it deletes itself
user = null;
// console.log(user); //Outputs "null"
