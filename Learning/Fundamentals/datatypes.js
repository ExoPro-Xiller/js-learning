// Strings and numbers
    let str = "This is a string";
    let num = 321; //This is a number
    let num_2 = 3432
    let int = 321n;
    // console.log(int);
    // console.log(str);
    // console.log(num);

// Using ${}
    // console.log(`the sum of num and num_2 is ${num + num_2}`);

// Boolean Expressions
    let truen = 5;
    let falsen = 10;
    // console.log(falsen > truen);
    // console.log(`${falsen >= truen}`);

//The Default value is of a variable is undefined so we use null
    let value_2 = undefined;
    let value = null;
    // console.log(value , value_2);

// typeof datatype
/* 
    console.log(typeof(123));
    console.log(typeof("Hello"));
    console.log(typeof(321n));
    console.log(typeof(null));
*/
    let name = "Ahmad";
/*
    console.log(`hello ${name}`);
*/