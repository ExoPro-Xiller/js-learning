// If else but with "?"

//Syntax : (condition) ? true : false
/*
    let marks = prompt("Enter Your Marks out of 50", );
    let message = (marks < 10) ? "Retarded" : (marks >= 30) ? "you can do better" : (marks > 40) ? "Great" : "";
    console.log(`${message}`);

    let compcreate = prompt("Which Company created JavaScript", );
    let messagejava = (compcreate == 'NetScape' || compcreate == 'netscape') ?
                            "Correct" : "Wrong";

    console.log(`${messagejava}`);
*/

//if else with if(condition){code}else{code}

/*
    let age  = prompt("Enter Your Age");
    if(age <= 13){
        document.getElementById('age').innerHTML = `You are a Todler, Age : ${age}`;
    }
    else if(age >= 14 && age < 18){
        document.getElementById('age').innerHTML = `You are a Teenager, Age : ${age}`;
    }
    else if(age >= 18){
        document.getElementById('age').innerHTML = `You are an Adult, Age : ${age}`;
    }
*/
