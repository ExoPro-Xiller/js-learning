//Operators in js(Math)
    let a = 5 + 8;  //Addition
    let b = 32 - 21;    //Subtraction
    let c = 5 * 6;  //Multiplication
    let d = 10 / 2;     //Divivsion
    let e = 5 % 2;  // Remainders
    let f = 3 ** 2; //Exponents

//String Concatenation
    let str1 = "Hello";
    let str2 = "World";
    // console.log(str1 + " " + str2);

//IF one of the operand is a string, the other one also becomes a string for addition or concatenation
//if one of the operand is a string and the other is a number, the string gets converted to a number in minus sign
    let strnumb = "1";
    let numb = 32;
    // console.log(strnumb + numb);
    // console.log(strnumb - numb);

//conversion of string values to numbers
    let wallet = 100;
    let wallet2 = 200;
    // console.log(+wallet + +wallet2);

    let n = 20;
    n += 10;
    n *= 2;
    // console.log(n)

    let counter = 0;
    counter++;//used to display the original value of variable before increment
    // console.log(counter);

    let anothercounter = 0;
    ++anothercounter;//Used to display the value of the variable after increment
    // console.log(anothercounter);






